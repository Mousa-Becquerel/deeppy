# Core
pydantic>=2.7,<3
# Upper-bound below 2.0 so we don't silently pick up a major API break like
# the OpenAIModel → OpenAIChatModel rename that shipped mid-1.x.
pydantic-ai>=1.107,<2.0
python-dotenv>=1.0

# Google Vertex AI (Gemini) — kept for fallback
google-auth>=2.29
google-cloud-aiplatform>=1.50

# API server
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.18

# PDF processing
pypdf>=4.0          # text extraction + page filtering for large PDFs
openpyxl>=3.1       # ontology Excel + user BoM parsing

# Persistence (SQLite now, Postgres-ready via SQLAlchemy)
SQLAlchemy>=2.0
alembic>=1.13
# Postgres driver (only used when DATABASE_URL is postgresql://). Bundling it
# now so the dev image is production-ready without a rebuild.
psycopg[binary]>=3.2

# Authentication (self-hosted JWT + bcrypt)
bcrypt>=4.1
PyJWT>=2.8

# Assistant (OpenAI provider for pydantic-ai chat agent; extraction stays on Gemini)
openai>=1.40

# Test suite
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
